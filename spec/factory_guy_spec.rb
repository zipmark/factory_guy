require 'spec_helper'

describe FactoryGuy do
  it 'has a version number' do
    expect(FactoryGuy::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
